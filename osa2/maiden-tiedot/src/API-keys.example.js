/**
 * API-keys
 *
 * Usage:
 *   - Make a copy of this file, rename it to `API-keys.js`
 *   - Visit https://www.apixu.com/my/ to create your free API key
 *   - In the APIkeys object below, replace `YOUR KEY HERE` with your actual API key
 *   - Use the app!
 *
 * NOTE: API-keys.js is added to gitignore, so your secrets aren't revealed by git history.
 */
const APIkeys = {
  'apixu': 'YOUR KEY HERE'
};

export default APIKeys;
